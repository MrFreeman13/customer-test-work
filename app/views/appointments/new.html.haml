%h2
  = "#{@user.name}, "
  please select a specialist
= render 'form'
.container

  - if flash[:notice]
    .notice
      = flash[:notice]
  = session[:appointment_step].inspect
  = session[:appointment_params].inspect